import { AppDispatch } from "@/sp/redux/store";

export const UserAuthApi = {
  Sign_In: async () => {},
  Sign_Up: async () => {},
  Sign_Out: async (dispath: AppDispatch, Sign_Out_Success: any) => {},
};
